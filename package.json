{
  "name": "carevo-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "echo 'static site' && exit 0"
  },
  "dependencies": {
    "@netlify/neon": "^0.1.0"
  },
  "devDependencies": {
    "netlify-cli": "^23.5.0"
  }
}
